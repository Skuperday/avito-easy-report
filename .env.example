# Avito Easy Report — environment variables

# Server
PORT=8080

# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_USER=avito
DB_PASSWORD=***DB_NAME=avito

# JWT Secret (generate: openssl rand -base64 32)
JWT_SECRET=***# CORS origin (use * for development)
CORS_ORIGIN=*
